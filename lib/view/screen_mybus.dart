import 'package:flutter/widgets.dart';

class ScreenMyBus extends StatelessWidget {
  const ScreenMyBus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
